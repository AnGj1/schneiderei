// api/controllers/DashboardController.js

module.exports = {
   /* showDashboard: function(req, res) {
        // Überprüfen, ob der Benutzer angemeldet ist
        if (!req.session.userId) {
          // Benutzer ist nicht angemeldet, weiterleiten zur Anmeldeseite
          return res.redirect('/login');
        }
    
        // Benutzer ist angemeldet, die Dashboard-Seite anzeigen
        return res.view('pages/dashboard');
      }*/
  };
  