module.exports = {
   /* login: function (req, res) {
      // Überprüfe Benutzername und Passwort
      // Wenn die Anmeldeinformationen gültig sind, setze eine Sitzungsvariable
      req.session.userId = 123;
      return res.ok();
    },
  
    dashboard: function (req, res) {
      // Überprüfe, ob der Benutzer angemeldet ist
      if (req.session.userId) {
        // Benutzer ist angemeldet, zeige das Dashboard an
        return res.view('dashboard');
      } else {
        // Benutzer ist nicht angemeldet, leite zur Anmeldeseite weiter
        return res.redirect('/login');
      }
    },
  
    logout: function (req, res) {
      // Lösche die Sitzungsvariable und leite zur Anmeldeseite weiter
      delete req.session.userId;
      return res.redirect('/login');
    },*/
  };
  